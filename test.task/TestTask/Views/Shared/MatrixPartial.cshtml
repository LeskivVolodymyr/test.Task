<table class="table table-bordered">

    @for (var i = 0; i < Model.GetLength(0); i++)
    {
        <tr>
            @for (var j = 0; j < Model.GetLength(0); j++)
            {
                <td>@Model[i][j]</td>
            }
            
        </tr>
    }
</table>